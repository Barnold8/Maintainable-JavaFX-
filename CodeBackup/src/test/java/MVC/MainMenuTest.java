package MVC;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MainMenuTest {

    // Both are void functions with massive functionality outside the scope
    // of what's possible in the coursework
}