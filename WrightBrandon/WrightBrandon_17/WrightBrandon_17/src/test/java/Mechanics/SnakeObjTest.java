package Mechanics;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SnakeObjTest {

    // Abstract methods are get and set and two void draw funcs which are
    // overridden from the JAVAFX lib so we don't need to test these
}