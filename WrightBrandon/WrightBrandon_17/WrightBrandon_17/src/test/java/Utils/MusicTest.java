package Utils;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MusicTest {

    // Class has one setter and one function that doesn't
    // provide much data, no need to test
}